import { BlockContentToTextPipe } from './block-content-to-text.pipe';

describe('BlockContentToTextPipe', () => {
  it('create an instance', () => {
    const pipe = new BlockContentToTextPipe();
    expect(pipe).toBeTruthy();
  });
});
